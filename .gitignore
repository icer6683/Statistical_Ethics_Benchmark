.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.DS_Store

# Run outputs: transcripts, workspace snapshots, scores, summaries, coding sheets.
# These are local records of real API calls; commit them deliberately if a run is to be shared.
results/
